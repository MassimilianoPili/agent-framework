// Generated by agent-compiler-maven-plugin from fe-svelte.agent.yml. DO NOT EDIT.
package com.agentframework.workers.generated.fesvelteworker;

import com.agentframework.worker.LlmWorker;
import com.agentframework.worker.WorkerMetadata;
import com.agentframework.worker.claude.WorkerChatClientFactory;
import com.agentframework.worker.context.AgentContextBuilder;
import com.agentframework.worker.interceptor.WorkerInterceptor;
import com.agentframework.worker.messaging.WorkerResultProducer;
import org.springframework.stereotype.Component;

import javax.annotation.processing.Generated;
import java.util.List;

/**
 * Frontend Svelte Worker.
 *
 * <p>Handles Svelte/SvelteKit frontend tasks — components with runes ($state, $derived), reactive stores, server-side rendering, form actions, and Vitest tests.</p>
 *
 * <p>Auto-generated from agent manifest: {@code fe-svelte.agent.yml}.</p>
 */
@Component
@Generated("agent-compiler-maven-plugin")
@WorkerMetadata(
    workerType = "FE",
    workerProfile = "fe-svelte",
    systemPromptFile = ".claude/agents/fe/SKILL.md",
    toolAllowlist = { "fs_list", "fs_read", "fs_write", "fs_search", "fs_grep", "bash_execute" },
    skillPaths = { "skills/crosscutting/" }
)
public class FeSvelteWorker extends LlmWorker {

    private static final String INSTRUCTIONS = """
            Implement the frontend task using Svelte 5 and SvelteKit.
            
            Follow these principles:
            - Svelte 5 runes: $state, $derived, $effect for reactivity (no legacy stores API)
            - SvelteKit conventions: +page.svelte, +layout.svelte, +server.ts
            - Server-side rendering (SSR) by default, client-side hydration
            - Form actions for mutations, load functions for data fetching
            - TypeScript for all logic files (.ts, +page.server.ts)
            - CSS scoped to components by default, CSS custom properties for theming
            - Accessibility: use:action for a11y, proper ARIA, keyboard navigation
            - Testing: Vitest for unit tests, Playwright for e2e
            - Performance: code splitting via dynamic imports, preloading
            
            Use the available filesystem tools to read existing code,
            understand the project structure, and write the implementation.""";

    private static final String RESULT_SCHEMA = """
            {
              "files_created": ["list of new files created"],
              "files_modified": ["list of existing files modified"],
              "summary": "brief description of what was implemented",
              "test_results": "description of tests written and their status"
            }""";

    public FeSvelteWorker(AgentContextBuilder contextBuilder,
                                  WorkerChatClientFactory chatClientFactory,
                                  WorkerResultProducer resultProducer,
                                  List<WorkerInterceptor> interceptors) {
        super(contextBuilder, chatClientFactory, resultProducer, interceptors);
    }

    @Override
    protected String instructions() {
        return INSTRUCTIONS;
    }

    @Override
    protected String resultSchema() {
        return RESULT_SCHEMA;
    }
}
