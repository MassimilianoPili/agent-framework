// Generated by agent-compiler-maven-plugin from fe-vanillajs.agent.yml. DO NOT EDIT.
package com.agentframework.workers.generated.fevanillajsworker;

import com.agentframework.worker.AbstractWorker;
import com.agentframework.worker.ToolAllowlist;
import com.agentframework.worker.WorkerExecutionException;
import com.agentframework.worker.claude.WorkerChatClientFactory;
import com.agentframework.worker.context.AgentContext;
import com.agentframework.worker.context.AgentContextBuilder;
import com.agentframework.worker.interceptor.WorkerInterceptor;
import com.agentframework.worker.messaging.WorkerResultProducer;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.model.ChatResponse;
import org.springframework.stereotype.Component;

import javax.annotation.processing.Generated;
import java.util.List;

/**
 * Frontend Vanilla JS Worker.
 *
 * <p>Handles vanilla HTML5, CSS, and JavaScript frontend tasks — semantic markup, responsive design, accessibility, performance optimization, and progressive enhancement. No frameworks or build tools unless explicitly requested.</p>
 *
 * <p>Auto-generated from agent manifest: {@code fe-vanillajs.agent.yml}.</p>
 */
@Component
@Generated("agent-compiler-maven-plugin")
public class FeVanillajsWorker extends AbstractWorker {

    private static final Logger log = LoggerFactory.getLogger(FeVanillajsWorker.class);

    private static final List<String> TOOL_ALLOWLIST = List.of(
        "fs_list",
        "fs_read",
        "fs_write",
        "fs_search",
        "fs_grep",
        "bash_execute"
    );

    private static final List<String> SKILL_PATHS = List.of(
        "skills/crosscutting/"
    );

    private static final String INSTRUCTIONS = """
            Implement the frontend task using vanilla HTML5, CSS, and JavaScript.
            Do NOT use any framework (React, Angular, Vue, Svelte) or build tool
            (Webpack, Vite, Rollup) unless the task explicitly requires it.
            
            Follow these principles:
            - Semantic HTML5: use <header>, <nav>, <main>, <section>, <article>, <footer>
            - CSS custom properties (--var) for theming, no preprocessors needed
            - Progressive enhancement: core content works without JS
            - Responsive design: mobile-first, CSS Grid/Flexbox, no media query frameworks
            - Accessibility: WCAG 2.1 AA, proper ARIA, keyboard navigation, contrast
            - Performance: lazy loading, font-display: swap, minimal DOM manipulation
            
            Use the available filesystem tools to read existing code,
            understand the project structure, and write the implementation.

            Return your result as JSON with the following structure:
            ```json
            {
              "files_created": ["list of new files created"],
              "files_modified": ["list of existing files modified"],
              "summary": "brief description of what was implemented",
              "test_results": "description of tests written and their status"
            }
            ```""";

    public FeVanillajsWorker(AgentContextBuilder contextBuilder,
                                     WorkerChatClientFactory chatClientFactory,
                                     WorkerResultProducer resultProducer,
                                     List<WorkerInterceptor> interceptors) {
        super(contextBuilder, chatClientFactory, resultProducer, interceptors);
    }

    @Override
    public String workerType() {
        return "FE";
    }

    @Override
    public String workerProfile() {
        return "fe-vanillajs";
    }

    @Override
    protected String systemPromptFile() {
        return ".claude/agents/fe/SKILL.md";
    }

    @Override
    protected ToolAllowlist toolAllowlist() {
        return new ToolAllowlist.Explicit(TOOL_ALLOWLIST);
    }

    @Override
    protected List<String> skillPaths() {
        return SKILL_PATHS;
    }

    @Override
    protected String execute(AgentContext context, ChatClient chatClient)
            throws WorkerExecutionException {

        String userPrompt = buildStandardUserPrompt(context, INSTRUCTIONS);
        log.info("[FE-Vanillajs] Executing task '{}' with {} dependency results",
                 context.taskKey(), context.dependencyResults().size());

        try {
            ChatResponse chatResponse = chatClient.prompt()
                .system(context.systemPrompt())
                .user(userPrompt)
                .call()
                .chatResponse();

            String response = chatResponse.getResult().getOutput().getText();
            if (chatResponse.getMetadata() != null && chatResponse.getMetadata().getUsage() != null) {
                recordTokenUsage(chatResponse.getMetadata().getUsage());
            }

            log.info("[FE-Vanillajs] Task '{}' completed, response length: {} chars",
                     context.taskKey(), response != null ? response.length() : 0);
            return response;

        } catch (Exception e) {
            throw new WorkerExecutionException(
                "FE-Vanillajs worker execution failed for task " + context.taskKey(), e);
        }
    }
}
