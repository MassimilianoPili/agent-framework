// Generated by agent-compiler-maven-plugin from mobile-swift.agent.yml. DO NOT EDIT.
package com.agentframework.workers.generated.mobileswiftworker;

import com.agentframework.worker.AbstractWorker;
import com.agentframework.worker.ToolAllowlist;
import com.agentframework.worker.WorkerExecutionException;
import com.agentframework.worker.claude.WorkerChatClientFactory;
import com.agentframework.worker.context.AgentContext;
import com.agentframework.worker.context.AgentContextBuilder;
import com.agentframework.worker.interceptor.WorkerInterceptor;
import com.agentframework.worker.messaging.WorkerResultProducer;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.model.ChatResponse;
import org.springframework.stereotype.Component;

import javax.annotation.processing.Generated;
import java.util.List;

/**
 * Mobile iOS (Swift) Worker.
 *
 * <p>Handles iOS mobile development — Swift 5.9+/6, SwiftUI, UIKit, async/await, SPM, XCTest, Xcode project configuration, and App Store submission preparation.</p>
 *
 * <p>Auto-generated from agent manifest: {@code mobile-swift.agent.yml}.</p>
 */
@Component
@Generated("agent-compiler-maven-plugin")
public class MobileSwiftWorker extends AbstractWorker {

    private static final Logger log = LoggerFactory.getLogger(MobileSwiftWorker.class);

    private static final List<String> TOOL_ALLOWLIST = List.of(
        "fs_list",
        "fs_read",
        "fs_write",
        "fs_search",
        "fs_grep",
        "bash_execute"
    );

    private static final List<String> SKILL_PATHS = List.of(
        "skills/crosscutting/"
    );

    private static final String INSTRUCTIONS = """
            Implement the mobile task using Swift for iOS.
            
            Follow these principles:
            - Swift 5.9+ / Swift 6: strict concurrency, @Observable, async/await
            - SwiftUI first, UIKit when needed (complex layouts, legacy integration)
            - MVVM or TCA architecture, clean separation of concerns
            - Swift Package Manager (SPM) for dependencies
            - XCTest + Swift Testing for unit and UI tests
            - Codable for JSON serialization, URLSession for networking
            - SwiftData or Core Data for persistence
            - Accessibility: VoiceOver, Dynamic Type support

            Return your result as JSON with the following structure:
            ```json
            {
              "files_created": ["list of new files created"],
              "files_modified": ["list of existing files modified"],
              "git_commit": "suggested commit message",
              "summary": "brief description of what was implemented",
              "test_results": {
                "total": 0,
                "passed": 0,
                "failed": 0,
                "skipped": 0
              }
            }
            ```""";

    public MobileSwiftWorker(AgentContextBuilder contextBuilder,
                                     WorkerChatClientFactory chatClientFactory,
                                     WorkerResultProducer resultProducer,
                                     List<WorkerInterceptor> interceptors) {
        super(contextBuilder, chatClientFactory, resultProducer, interceptors);
    }

    @Override
    public String workerType() {
        return "MOBILE";
    }

    @Override
    public String workerProfile() {
        return "mobile-swift";
    }

    @Override
    protected String systemPromptFile() {
        return ".claude/agents/mobile-swift/SKILL.md";
    }

    @Override
    protected ToolAllowlist toolAllowlist() {
        return new ToolAllowlist.Explicit(TOOL_ALLOWLIST);
    }

    @Override
    protected List<String> skillPaths() {
        return SKILL_PATHS;
    }

    @Override
    protected String execute(AgentContext context, ChatClient chatClient)
            throws WorkerExecutionException {

        String userPrompt = buildStandardUserPrompt(context, INSTRUCTIONS);
        log.info("[MOBILE-Swift] Executing task '{}' with {} dependency results",
                 context.taskKey(), context.dependencyResults().size());

        try {
            ChatResponse chatResponse = chatClient.prompt()
                .system(context.systemPrompt())
                .user(userPrompt)
                .call()
                .chatResponse();

            String response = chatResponse.getResult().getOutput().getText();
            if (chatResponse.getMetadata() != null && chatResponse.getMetadata().getUsage() != null) {
                recordTokenUsage(chatResponse.getMetadata().getUsage());
            }

            log.info("[MOBILE-Swift] Task '{}' completed, response length: {} chars",
                     context.taskKey(), response != null ? response.length() : 0);
            return response;

        } catch (Exception e) {
            throw new WorkerExecutionException(
                "MOBILE-Swift worker execution failed for task " + context.taskKey(), e);
        }
    }
}
