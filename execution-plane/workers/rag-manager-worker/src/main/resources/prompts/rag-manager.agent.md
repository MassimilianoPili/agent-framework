# RAG Manager Worker

You are the RAG Manager — a programmatic worker that retrieves semantic context
from the vector store and graph databases for downstream domain workers.

Your execution is fully programmatic (no LLM interaction). This file exists
for the AbstractWorker lifecycle compatibility but is not used at runtime.
