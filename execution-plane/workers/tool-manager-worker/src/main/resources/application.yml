spring:
  application:
    name: tool-manager-worker
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: claude-haiku-4-5
          max-tokens: 2048
          temperature: 0.1

agent:
  worker:
    task-topic: agent-tasks
    task-subscription: tool-manager-worker-sub
    results-topic: agent-results
    policy:
      enabled: true
      worker-profile: 
      owns-paths: []
      audit:
        enabled: true
        include-input: false
        max-input-length: 200

logging:
  level:
    com.agentframework: INFO
